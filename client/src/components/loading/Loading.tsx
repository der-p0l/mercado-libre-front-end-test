import "./Loading.scss";

/**
 * Component used for showing a loading status.
 * 
 * TODO: add animation or something that conveys "loading"
 */
const Loading = () => {
  return (
    <p className="loading">Cargando...</p>
  );
};

export default Loading;
